name: HEAD_YOLOv8n_SCUT_Nano_416

# Exported from: mvp/models/src/yolov8_head_detector/yolov8_head_scut_nano.pt
input_model: models/yolov8_head_scut_nano_416.onnx

# Ultralytics YOLO expects float input in [0..1] RGB.
# ModelConverter will bake preprocessing so the compiled RVC2 model can accept uint8 BGR frames.
mean_values: [0, 0, 0]
scale_values: [255, 255, 255]

inputs:
  - name: images
    shape: [1, 3, 416, 416]
    layout: NCHW

outputs:
  - name: output0
