name: HEAD_YOLOv8n_SCUT_Nano_416_YOLO_RAW_SWAPPED

# Exported from: mvp/models/src/yolov8_head_detector/yolov8_head_scut_nano.pt
# Patched outputs: swap Concat order so cls is before reg (some parsers expect this ordering).
input_model: models/yolov8_head_scut_nano_416_yolo_raw_swapped.onnx

mean_values: [0, 0, 0]
scale_values: [255, 255, 255]

inputs:
  - name: images
    shape: [1, 3, 416, 416]
    layout: NCHW

outputs:
  - name: cat_13_swapped
  - name: cat_14_swapped
  - name: cat_15_swapped
