# Calibration Checklist

1. Service `passengers-camera-depth-counting.service` is `active`.
2. `messages` in `/health` increases continuously.
3. `detections` increases when person is visible in scene.
4. `count_in/count_out` changes only on real line crossing.
5. Updated parameters are recorded in camera runbook.
6. Next step + purpose documented.
